---
name: project-system-design-learning
description: "Arun's system design study plan + progress tracker (FAANG SDE-2 prep)"
metadata: 
  node_type: memory
  type: project
  originSessionId: 102a35a0-b30b-4868-8cb3-686b695f8a3c
---

Teaching Arun system design from scratch for FAANG SDE-2 (started 2026-06-18). **Preferred format (chosen 2026-06-18): INTERVIEW-CRAM depth** — must-know points + exact interview Q&A, minimal theory. Reinforce with: self-check questions (quiz + grade), real design problems (URL shortener, Twitter, chat as we go), and heavy diagrams + back-of-envelope numbers (latency, QPS, storage). One concept per session. Supports [[project_faang_roadmap]]; he has gaps in system design.

**Curriculum order:** (1) Scaling vertical/horizontal ✅ DONE, (2) Latency/Throughput/Bandwidth, (3) Availability/Reliability/Fault tolerance + the 9s, (4) CAP/PACELC, (5) Load Balancers ← NEXT, (6) Caching, (7) SQL vs NoSQL, (8) Replication, (9) Sharding, (10) Indexing, (11) CDN, (12) Message queues/pub-sub, (13) Consistency models, (14) Rate limiting, (15) Consistent hashing, (16) Monolith vs microservices, (17) API design REST/gRPC/GraphQL, (18) Proxy vs reverse proxy, (19) ACID/BASE, (20) Interview framework, (21) Practice problems (URL shortener → Twitter → chat → YouTube → Uber).

**Progress (as of 2026-06-18):** Topics 1 (scaling), 2 (load balancers), 3 (caching) COMPLETE — Arun scored well on all self-checks (only weak spot: phrasing *why* cache invalidation is hard = two sources of truth, freshness-vs-perf tradeoff). Topic 4 (SQL vs NoSQL) cheat card delivered; Arun went to lunch, will return to do its self-check. **Resume: grade Topic 4 self-check, then Topic 6 Caching is done so next new topic = Replication (Topic 8) ... actual next per curriculum after SQL/NoSQL = Replication.** Recommended pace agreed: 2-3 topics/day + spaced recall (re-quiz old topics at start of each session). Done 3 topics on day 1.

**PLAN CHANGE (2026-06-18):** After Topic 4, Arun wants to switch to LIVE MOCK DESIGN ROUNDS where I act as the interviewer (e.g. "Design YouTube with 100k concurrent live users"). Approach: I drive it like a real 45-min FAANG round — make him clarify requirements, do back-of-envelope math (QPS/storage/bandwidth), draw architecture, then throw follow-up curveballs. Teach missing building-block topics (CDN, message queues, etc.) INLINE as they come up in the design rather than as standalone cards. First mock = Design YouTube/live streaming.

**MOCK 1 RESULT (Design YouTube, 2026-06-18):** Arun scored LEAN HIRE for SDE-2 on first attempt. Strengths: excellent clarifying/scoping, reasoned out CAP/eventual-consistency himself, caught transcoding + content moderation unprompted, named CDN instantly, got the live-stream CDN fan-out idea (one origin fetch per segment serves all 100k viewers). Weak spots to DRILL: (1) estimation solo — turning users→QPS→storage without a walkthrough; (2) where data lives — kept wanting to put video chunks in Redis; rule learned: Redis=small/hot metadata, Object Storage+CDN=big video files. Learned-in-context (he resisted dry study, learns by doing): Message Queues (async transcoding), SQL vs NoSQL (NoSQL for metadata, polyglot persistence), CDN, adaptive bitrate HLS/DASH, live streaming RTMP→ingest→transcode→segments. Flagged for later: WebSockets+PubSub for live chat. **MOCK 2 RESULT (Design URL shortener, 2026-06-18):** Arun scored HIRE for SDE-2 (up from Lean Hire). Also covered cheat cards for Replication + Sharding (3/3 self-checks each, minor terminology slips: said "consistent caching" for hashing; flipped "avoid" vs "use" consistent hashing). BIG WIN: estimation went from weakest skill to 5/5 — owned write QPS (~1,160/s) and read QPS (~116k/s) solo; learned the 86,400≈100k rounding trick. Learned-in-context: how to derive requirements (4-question NFR checklist = Available? Latency? Consistency? Read/Write ratio?), Base62 (URL-safe, NOT Base64) counter+encoding for unique short codes (no collision check needed), Snowflake/ticket-server distributed ID generation, 62^7≈3.5T sizing. Weak spots still to drill: (1) reflex to check CACHE FIRST on read-heavy paths (forgot it on redirect flow), (2) Base62≠Base64, (3) 301 vs 302 redirect tradeoff (302 for click analytics). **MOCK 3 RESULT (Design Twitter, 2026-06-18):** Arun scored HIRE for SDE-2 (3rd round holding Hire; this was the hardest). NFR 4-question checklist now automatic. Learned the FAN-OUT pattern: PULL (fan-out on read = cheap writes/slow reads) vs PUSH (fan-out on write = fast reads/expensive writes) → HYBRID = push for normal users, PULL celebrity tweets on read + merge by time (avoids 100M-write fan-out). Caching: Redis holds precomputed timelines (push) + caches hot tweets (cache-aside on read). DB: Cassandra for tweets, Redis for timelines. KEY ESTIMATION LESSON finally locked: 'Rate (QPS) = daily ÷ 86,400; Storage = daily × days; YEARS only touch storage' — he hit the rate-vs-storage trap twice but self-corrected both times (strong sign). Also clarified cache(Redis ~1ms, hot keys) vs read-replica(follower DB ~10ms, full copy) are SEPARATE complementary layers — he'd conflated them. **Carry-forward drills: (1) untangle push/pull wording (pull=fetch on read, don't pre-build), (2) cache-first reflex on reads. Next session: quick spaced-recall, then Mock 4 — suggest a chat app (WebSockets + pub/sub, new pattern) or Instagram (reuses fan-out). After ~1-2 more mocks, cover the formal interview framework (roadmap Topic 20).**

**MOCK 4 RESULT (Design WhatsApp/chat, 2026-06-18):** Arun scored STRONG HIRE for SDE-2 — his best round. Estimation now fully reflexive (~700k QPS instantly: 300M×200/86400). Caught E2E encryption unprompted (senior). New patterns learned/confirmed (he'd pre-studied these well): WebSockets (persistent full-duplex connection so server PUSHES in real-time vs HTTP request-response; alts = long-polling/SSE), connection/session registry in Redis (userID→serverID), offline handling (not in registry → persist to DB inbox → deliver on reconnect). Only gap: the inter-server delivery link — Server1 reaches Server47 via PUB/SUB (Kafka/Redis), not direct calls. NFR sharpening: for chat say 'reliable ordered delivery' not 'strong consistency'. Stack: Cassandra (messages), Redis (registry), Kafka/Redis pub-sub (inter-server), ticks = sent/delivered/read statuses flow back over sender's socket. **Running tally: Mock1 YouTube=Lean Hire, Mock2 URL-shortener=Hire, Mock3 Twitter=Hire, Mock4 chat=Strong Hire — clear upward trend. Next: 1 more mock (suggest Instagram to reuse fan-out, or rate limiter / Google Drive for variety), THEN the formal 45-min interview framework (roadmap Topic 20) to tie it together.**

**TODO (deferred, bad internet 2026-06-18):** Build Arun a comprehensive System Design Q&A Cheat Sheet as an Artifact (keeper reference covering ALL topics) when his connection is back. Design plan already chosen: blueprint/spec-sheet aesthetic, paper #F4F6F9, accent engineering-blue #1E5F8C, monospace eyebrows/terms, sticky topic index, Q&A cards. Did a full mixed-topic recall quiz in chat instead (offline-friendly).

**CHEAT SHEET BUILT (2026-06-18):** Published comprehensive System Design Q&A Artifact at https://claude.ai/code/artifact/a9b288b3-1c86-447f-beac-bcc4b770eb09 (blueprint spec-sheet design, all 11 topics + estimation rules + decision heuristics + his mock track record). Source in scratchpad system-design-cheatsheet.html — redeploy to same URL to update.

**MASTER QUIZ RESULT (2026-06-18, from memory):** Arun scored 16/20. Strong recall on hard topics (replication lag, sharding strategies, WebSockets, ACID). The 4 misses were ALL adjacent-concept BLENDING — his key weakness: he conflates stateless / replicas / sharding / caching / SQL-vs-NoSQL because they sit near each other mentally. Specific errors: Q2 (said 'replicas' for what-makes-horizontal-scaling-easy; answer=STATELESS), Q3 (missed HEALTH CHECKS as the key LB job), Q9 (said 'NoSQL for stateless servers'; answer=SQL for relational/ACID, NoSQL for scale/flexible), Q13 (said 'caching' for replication-vs-sharding; answer=replication copies same data/sharding splits different data). **DRILL next session: the 5-layer separation table — Stateless(app servers)=horizontal scaling; Replicas=scale reads+HA; Sharding=scale writes; Caching=speed+shield DB; SQL-vs-NoSQL=data shape. Re-quiz just these 5 distinctions before moving to Mock 5 / interview framework.**

**5-LAYER DRILL (2026-06-18):** Scored 3.5/6 — but ALL misses were the SAME root error: the word **"stateless"** keeps attaching to the wrong concept. He put it on VERTICAL scaling (Drill 1) and on NoSQL (Drill 5). His confirmed weak pattern: 'stateless' is his trap word. THE FIX HE NEEDS DRILLED: '"Stateless" = app servers holding no local session → HORIZONTAL scaling. That is its ONLY home — never vertical, never a database property.' Also taught: Redis does TWO distinct jobs — (a) cache hot DB data (fixes read load), (b) session store (makes servers stateless, fixes logout-on-scale) — same tool, different purpose; he blurred these. Got right: read replicas→scale reads, sharding→scale writes, caching→fast reads+shield DB, SQL=relational/ACID. **Next session: re-test 'stateless' in isolation FIRST (cold), confirm it's sealed, then proceed to Mock 5 (Instagram/rate limiter) or interview framework.**

**MOCK 5 RESULT (Design Rate Limiter, 2026-06-18):** Arun scored HIRE for SDE-2 (5 rounds, 5 hires). HE HAS REAL EXPERIENCE: built rate limiting in his HRMS (300 req/min → 429). BIG WIN: used 'stateless' CORRECTLY and unprompted (Redis centralized counter shared across servers = same pattern as session store) — the leak drilled earlier THIS session is now SEALED, confirmed via 2 isolation checks first. Learned: rate limiter sits at EDGE (before app logic, fast reject), Redis centralized counter with atomic INCR, Token Bucket algorithm (capacity + steady refill, fixes fixed-window boundary-burst, stores token_count+last_refill_timestamp in Redis), fail-open vs fail-closed (graceful degradation under Fault Tolerance topic; chose fail-open + alert + coarse in-memory fallback). NEW WATCH-ITEM: his concept-BLENDING reflex moved from 'stateless' to 'cache stampede' (wrongly attached it to token bucket). Not a knowledge gap — a slow-down-and-verify-the-term-belongs habit to build. **Next: consider the 45-min interview FRAMEWORK (roadmap Topic 20) to teach choreography/timing — he has all the building blocks now and 5 solid mocks. Or Mock 6 (Instagram/Google Drive/Notification system) for variety.**
