{# templates/why.j2 — "why this company" answer.
   -------------------------------------------------------------------------
   THIS FILE IS A SCAFFOLD AND IS REFUSED AS-IS.

   apply/templates.py rejects any template still containing a TODO marker, so
   an application needing this answer will park in the review queue until you
   replace the prose below with your own words. That is deliberate: the design
   rule in state/answers.py is that you compose, the agent only selects. A
   generated cover letter is exactly the thing this system is built not to send.

   Variables the runner supplies (pass them on the command line):
     company  --company "Acme"     required
     role     --role "Senior FE"   required
   Anything else you reference raises UndefinedError and parks the application,
   rather than rendering a blank half-sentence.
   -------------------------------------------------------------------------#}
TODO: replace this entire block with your own two or three sentences on why
{{ company }} specifically, and why the {{ role }} role. Keep it true and
concrete — name the product, the stack, or the problem you actually care about.
